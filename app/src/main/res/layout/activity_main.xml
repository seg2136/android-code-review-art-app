<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:paddingLeft="@dimen/activity_horizontal_margin"
                android:paddingRight="@dimen/activity_horizontal_margin"
                android:paddingTop="@dimen/activity_vertical_margin"
                android:paddingBottom="@dimen/activity_vertical_margin"
                tools:context=".MainActivity">

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Gallery Events and Openings"
        android:id="@+id/eventsButton"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:background="#c2111d"
        android:textColor="@android:color/white"
        android:textStyle="bold"
        android:textSize="16sp"
        android:typeface="serif"/>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Artists"
        android:id="@+id/artistsButton"
        android:background="#7b1019"
        android:layout_above="@+id/eventsButton"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_marginBottom="21dp"
        android:textColor="@android:color/white"
        android:textSize="16sp"
        android:typeface="serif"
        android:textStyle="bold"/>

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/mainImage"
        android:layout_centerHorizontal="true"
        android:src="@drawable/white_pink"
        android:layout_above="@+id/mainLabel"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Where have you seen art today?"
        android:id="@+id/mainLabel"
        android:layout_above="@+id/artistsButton"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="74dp"
        android:textSize="16sp"
        android:layout_marginTop="10dp"
        android:typeface="serif"
        android:textColor="#4f4f4f"/>
</RelativeLayout>
